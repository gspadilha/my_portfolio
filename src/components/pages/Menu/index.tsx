import React from 'react';

import { MenuContainer } from './styles';

const Menu = () => {
  return (
    <MenuContainer>
      <div>Sobre</div>
      <div>Contato</div>
    </MenuContainer>
  );
};

export default Menu;
